import QtQuick

// Filled out in K4-06 vertical slices. Keeping the view as a named seam from
// the first panel-host commit prevents panel ownership logic from depending on
// the eventual control/detail composition.
Item {
    required property var plugin
}
